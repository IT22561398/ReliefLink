apiVersion: v1
kind: Service
metadata:
  name: request-service
  namespace: relieflink
  labels:
    app: request-service
spec:
  selector:
    app: request-service
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: request-service
  namespace: relieflink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: request-service
  template:
    metadata:
      labels:
        app: request-service
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - request-service
              topologyKey: kubernetes.io/hostname
      initContainers:
      - name: migrate
        image: 489109585253.dkr.ecr.ap-south-1.amazonaws.com/relieflink-request-service:latest
        imagePullPolicy: Always
        command: ["sh", "-c", "until nc -z postgres-0.postgres 5432; do echo waiting for postgres; sleep 2; done; ./node_modules/.bin/prisma migrate deploy --schema=prisma/schema.prisma"]
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: POSTGRES_PASSWORD
        - name: REQUEST_DATABASE_URL
          value: "postgresql://postgres:$(DB_PASSWORD)@postgres-0.postgres:5432/request_service_db"
      containers:
      - name: request-service
        image: 489109585253.dkr.ecr.ap-south-1.amazonaws.com/relieflink-request-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3004
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: NODE_ENV
        - name: PORT
          value: "3004"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: JWT_SECRET
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: LOG_LEVEL
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: POSTGRES_PASSWORD
        - name: REQUEST_DATABASE_URL
          value: "postgresql://postgres:$(DB_PASSWORD)@postgres-0.postgres:5432/request_service_db"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: REDIS_PASSWORD
        - name: REDIS_URL
          value: "redis://:$(REDIS_PASSWORD)@redis-0.redis:6379"
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: AUTH_SERVICE_URL
        - name: NOTIFICATION_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: NOTIFICATION_SERVICE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "384Mi"
            cpu: "300m"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: request-service-hpa
  namespace: relieflink
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: request-service
  minReplicas: 1
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
