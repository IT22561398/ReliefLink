apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: relieflink
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: relieflink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - notification-service
              topologyKey: kubernetes.io/hostname
      initContainers:
      - name: migrate
        image: 489109585253.dkr.ecr.ap-south-1.amazonaws.com/relieflink-notification-service:latest
        imagePullPolicy: Always
        command: ["sh", "-c", "./node_modules/.bin/prisma migrate deploy --schema=prisma/schema.prisma"]
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: POSTGRES_PASSWORD
        - name: NOTIFICATION_DATABASE_URL
          value: "postgresql://postgres:$(DB_PASSWORD)@postgres-0.postgres:5432/notification_service_db"
      containers:
      - name: notification-service
        image: 489109585253.dkr.ecr.ap-south-1.amazonaws.com/relieflink-notification-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3002
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: NODE_ENV
        - name: PORT
          value: "3002"
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: LOG_LEVEL
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: POSTGRES_PASSWORD
        - name: NOTIFICATION_DATABASE_URL
          value: "postgresql://postgres:$(DB_PASSWORD)@postgres-0.postgres:5432/notification_service_db"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: REDIS_PASSWORD
        - name: REDIS_URL
          value: "redis://:$(REDIS_PASSWORD)@redis-0.redis:6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "384Mi"
            cpu: "300m"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-service-hpa
  namespace: relieflink
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-service
  minReplicas: 1
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
