apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: relieflink
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: relieflink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - auth-service
              topologyKey: kubernetes.io/hostname
      initContainers:
      - name: migrate
        image: 489109585253.dkr.ecr.ap-south-1.amazonaws.com/relieflink-auth-service:latest
        imagePullPolicy: Always
        command: ["sh", "-c", "./node_modules/.bin/prisma migrate deploy --schema=prisma/schema.prisma"]
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: POSTGRES_PASSWORD
        - name: AUTH_DATABASE_URL
          value: "postgresql://postgres:$(DB_PASSWORD)@postgres-0.postgres:5432/auth_service_db"
      containers:
      - name: auth-service
        image: 489109585253.dkr.ecr.ap-south-1.amazonaws.com/relieflink-auth-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: NODE_ENV
        - name: PORT
          value: "3001"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: JWT_SECRET
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: LOG_LEVEL
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: POSTGRES_PASSWORD
        - name: AUTH_DATABASE_URL
          value: "postgresql://postgres:$(DB_PASSWORD)@postgres-0.postgres:5432/auth_service_db"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: relieflink-secrets
              key: REDIS_PASSWORD
        - name: REDIS_URL
          value: "redis://:$(REDIS_PASSWORD)@redis-0.redis:6379"
        - name: VOLUNTEER_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: relieflink-config
              key: VOLUNTEER_SERVICE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "384Mi"
            cpu: "300m"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auth-service-hpa
  namespace: relieflink
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: auth-service
  minReplicas: 1
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
